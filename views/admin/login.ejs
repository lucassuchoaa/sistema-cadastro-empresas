<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login Administrativo</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <link href="/css/style.css" rel="stylesheet">
</head>
<body class="bg-light">
    <div class="container">
        <div class="row justify-content-center align-items-center min-vh-100">
            <div class="col-md-6 col-lg-4">
                <div class="card shadow">
                    <% 
                        const tipo = typeof query !== 'undefined' && query.tipo ? query.tipo : 'empresa';
                        const isSuperAdmin = tipo === 'superadmin';
                    %>
                    
                    <div class="card-header <%= isSuperAdmin ? 'bg-danger' : 'bg-primary' %> text-white text-center">
                        <h4 class="mb-0">
                            <% if (isSuperAdmin) { %>
                                <i class="fas fa-crown me-2"></i>Super Administrador
                            <% } else { %>
                                <i class="fas fa-building me-2"></i>Acesso Empresarial
                            <% } %>
                        </h4>
                    </div>
                    <div class="card-body">
                        <h4 class="card-title text-center mb-4">
                            <% if (isSuperAdmin) { %>
                                Login Super Admin
                            <% } else { %>
                                Login da Empresa
                            <% } %>
                        </h4>
                        
                        <% if (typeof error !== 'undefined') { %>
                            <div class="alert alert-danger" role="alert">
                                <%= error %>
                            </div>
                        <% } %>
                        
                        <% if (isSuperAdmin) { %>
                            <div class="alert alert-warning" role="alert">
                                <i class="fas fa-exclamation-triangle me-2"></i>
                                <strong>Área Restrita:</strong> Acesso apenas para super administradores.
                            </div>
                        <% } else { %>
                            <div class="alert alert-info" role="alert">
                                <i class="fas fa-info-circle me-2"></i>
                                <strong>Acesso Empresarial:</strong> Use o slug da sua empresa e a senha fornecida.
                            </div>
                        <% } %>
                        
                        <form method="POST" action="/admin/login">
                            <input type="hidden" name="tipo" value="<%= tipo %>">
                            
                            <div class="mb-3">
                                <label for="empresa" class="form-label">
                                    <% if (isSuperAdmin) { %>
                                        Usuário
                                    <% } else { %>
                                        Slug da Empresa
                                    <% } %>
                                </label>
                                <input type="text" class="form-control" id="empresa" name="empresa" required 
                                       placeholder="<%= isSuperAdmin ? 'superadmin' : 'slug-da-empresa' %>">
                            </div>
                            <div class="mb-3">
                                <label for="senha" class="form-label">Senha</label>
                                <input type="password" class="form-control" id="senha" name="senha" required>
                            </div>
                            <button type="submit" class="btn <%= isSuperAdmin ? 'btn-danger' : 'btn-primary' %> w-100">
                                <% if (isSuperAdmin) { %>
                                    <i class="fas fa-crown me-2"></i>Entrar como Super Admin
                                <% } else { %>
                                    <i class="fas fa-building me-2"></i>Entrar como Empresa
                                <% } %>
                            </button>
                        </form>
                    </div>
                </div>
                
                <div class="text-center mt-3">
                    <a href="/" class="btn btn-outline-secondary">← Voltar para seleção de acesso</a>
                </div>
            </div>
        </div>
    </div>
    
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>